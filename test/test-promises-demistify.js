'use strict';



import Test from 'tape';
import Promises-demistify from '../dist/lib';

Test('test promises-demistify', (t) => {

    t.test('plan', (t) => {
        t.plan(1);

        t.ok(true);
    });

});
